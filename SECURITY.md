# Security Policy

Please do not open a public issue containing passwords, API keys, tokens, private keys, exported profiles, server addresses, or diagnostic bundles with sensitive information.

For local development, keep secrets outside the repository. GraveOps user configuration is stored separately from the installed application.

This RC2 installer is not code-signed. Verify release checksums before execution.